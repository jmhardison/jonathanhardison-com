cd _site
git config --global user.email "nulluser@null.com"
git config --global user.name "Travis-ci build agent"
git init
git add .
git commit -a
git config git-ftp.user $USERNAME
git config git-ftp.url $HOST/site/wwwroot
git config git-ftp.password $PASSWORD
git ftp $DEPLOYTYPE -f
